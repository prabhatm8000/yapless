import ProfileCard from "../components/ProfileCard";

const ProfileView = () => {
    return (
        <div className="mt-40">
            <ProfileCard />
        </div>
    );
};

export default ProfileView;
